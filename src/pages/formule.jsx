import React from 'react';
import Layout from "../components/Layout/Layout";

const Formule = (props) => {
    return ( 
        <Layout>
            Formule
        </Layout> 
    );
}
 
export default Formule;